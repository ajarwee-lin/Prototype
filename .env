# JWT secret key for signing tokens
JWT_SECRET=your_jwt_secret_key

# MongoDB connection URI (or PostgreSQL if you're using that)
MONGO_URI=mongodb://localhost:27017/utechdb

# PORT for running the server
PORT=5000
